{{ form_start(form) }}
{{ form_widget(form) }}
<button class="text-xl bg-rose-700 shadow rounded-lg p-2 ml-4 mt-10 text-white hover:bg-rose-800 focus:ring-2 focus:ring-rose-900">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
